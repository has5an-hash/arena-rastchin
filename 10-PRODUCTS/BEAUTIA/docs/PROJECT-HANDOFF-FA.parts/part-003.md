ت، استایل و منو — ۷ سپتامبر ۲۰۲۶

- علت غیرفعال‌بودن هویت‌های بصری مشخص شد: `demo-identities.css` به `data-beautia-appearance="pastel"` وابسته بود اما این ویژگی روی سند تولید نمی‌شد. ویژگی به‌صورت کنترل‌شده برای صفحات دمو اضافه شد.
- `@font-face` فونت‌های Sahel و Gandom به `fonts.css` مشترک منتقل شد؛ دیگر فونت هویت‌ها به یک فایل بصری اختیاری وابسته نیست.
- تقدم `front-page.php` باعث می‌شد قالب صفحه Studio نادیده گرفته و ساختار قدیمی و یکسان نمایش داده شود. صفحه اصلی اکنون انتساب صریح Studio را رعایت می‌کند.
- cache bust مبتنی بر زمان تغییر فایل برای فونت، لایه polish، هویت دمو و اسکریپت اصلی اضافه شد تا مرورگر CSS/JS قدیمی را نگه ندارد.
- هدر روشن و خوانا، فاصله منظم، RTL، `z-index` پایدار و مگامنوی قابل استفاده با Hover/Focus پیاده شد.
- آیکن آیتم «خدمات» کوچک و درون‌خطی شد؛ اندازه تمام شش آیتم منوی دسکتاپ در هر هفت دمو 43.3px و یکسان است.
- منوی موبایل Hair در عرض 390px تست شد: شش آیتم اصلی ارتفاع یکسان 68.55px، RTL صحیح، overflow صفر و تصویر شکسته صفر دارد. هنگام بازبودن منو، Chat/FAB/Sticky CTA پنهان می‌شوند و `aria-hidden` به‌روز می‌شود.
- ممیزی خودکار صفحه اصلی هر هفت دمو در عرض 390 و 1440 پیکسل: فونت هویت فعال، هدر خوانا، overflow صفر و تصویر شکسته صفر. پوسته صفحه با viewport هم‌اندازه و تمام‌عرض است.
- تفاوت ساختاری فعلی کافی نیست: Nail دارای 11 سکشن و شش دموی دیگر دارای 10 سکشن با ترتیب تقریباً یکسان‌اند. تحقیق و طراحی تنوع حوزه‌محور همچنان باز است.

### تنوع حوزه‌محور و حذف تداخل محتوایی — ۷ سپتامبر ۲۰۲۶

- تحقیق رقبا و تصمیم معماری در `COMPETITOR-RESEARCH-FA.md` ثبت شد؛ نتیجه: Core مشترک می‌ماند اما ترتیب روایت، تعداد کارت‌ها و ترکیب قابلیت‌ها باید حوزه‌محور باشد.
- پروفایل مستقل صفحه اصلی برای هفت دمو پیاده شد. ترتیب سکشن‌ها و تعداد خدمات، نمونه‌کارها، اعضای تیم و مقالات دیگر یکسان نیست.
- Clinic با اعتماد و معرفی، Hair و Makeup با Portfolio، Spa با تجربه، Lashes با گالری فرم‌ها، Barber با خدمات و Nails با خدمات/مقایسه شروع می‌شوند.
- تداخل واقعی تصاویر بخش «درباره» حذف شد. هر دمو اکنون فقط Hero و Portfolio متعلق به حوزه خودش را در این بخش استفاده می‌کند.
- عنوان و سابقه بخش «درباره» نیز برای هر حوزه مستقل شد.
- این مرحله «نسخه اول تنوع ساختاری» است؛ ویجت‌های تخصصی مرحله دوم هنوز در چک‌لیست باز هستند.
- خروجی جدید در مرورگر هر هفت دمو تأیید شد: ترتیب سکشن‌ها متفاوت، عنوان و دو تصویر About کاملاً هم‌حوزه، overflow افقی صفر و تصویر واقعاً شکسته صفر است.
- منوی موبایل هر هفت دمو در عرض 390px جداگانه باز و اندازه‌گیری شد: جهت RTL، شش آیتم با ارتفاع یکسان 68.55px، آیتم «خدمات» دقیقاً هم‌اندازه بقیه، `aria-hidden=false` هنگام بازبودن و FAB پنهان است.
- سکشن تخصصی مستقل برای هر دمو ساخته شد: راهنمای طرح Nails، مسیر مراجعه Clinic، انتخاب مهارت Hair، آیین Spa، نقشه فرم چشم Lashes، پکیج مراسم Makeup و باشگاه Barber. تعداد کارت، محتوا، جایگاه و فرم بصری آن‌ها یکسان نیست.
- سکشن‌های تخصصی در عرض‌های 1280 و 390 پیکسل تست شدند: overflow صفر و نسبت کنتراست متن/کارت بین 9.17 و 11.56 است. متن کم‌رنگ Hair در همین ممیزی پیدا و اصلاح شد.
- ممیزی فعلی دیتابیس نشان داد تمام تصاویر خدمات، Portfolio و مقاله اکنون حوزه‌محورند؛ تنها تداخل تصویری باقی‌مانده، سه تصویر عمومی اعضای تیم است که بین چند دمو تکرار شده و باید با پرتره‌های اختصاصی جایگزین شود.
- مورد تصاویر تیم نیز بسته شد: هفت Triptych حوزه‌محور به ۲۱ پرتره 720×960/JPEG progressive تبدیل، در Assets هر هفت Core همگام، به رکوردهای Staff همان دیتابیس متصل و `staff_photo_pools` Seeder اصلاح شد.
- ممیزی دیتابیس پس از اتصال پرتره‌ها برای تصویر Service/Portfolio/Staff/Post مقدار `cross_demo_duplicate_images=0` داد. هر ۲۱ تصویر تیم نیز در مرورگر با ابعاد واقعی و بدون شکستگی بارگذاری شد.
- محتوای غیرتصویری تمام Testimonialها نیز هش شد و تکرار میان‌دمویی نداشت. سه محتوای تکراری باقی‌مانده مربوط به صفحه‌های Services/Portfolio/Blog بود که فقط Shortcode مشترک داشتند؛ برای هر دمو معرفی حوزه و نام مجموعه اضافه و Seeder نیز اصلاح شد.
- ممیزی نهایی محتوای غیرخالی Post/Page/CPT اکنون `duplicate_nonempty_content_groups=0` و `literal_slashes=0` می‌دهد.
- ۲۱ صفحه Services/Portfolio/Blog در مرورگر باز شد: عنوان مستقل، overflow صفر و تصویر شکسته صفر.
- ۳۵ صفحه About/Contact/Booking/Account/Login در هر هفت دمو، یک بار دسکتاپ و یک بار موبایل (۷۰ حالت) ممیزی شد: RTL صحیح، overflow صفر، تصویر شکسته صفر و هم‌پوشانی Sticky CTA/Support صفر.
- سطح کلیک آیتم‌های کوتاه منوی دسکتاپ حداقل 44px شد. در تأیید زنده، هر شش آیتم ارتفاع 43.3px دارند؛ «خدمات» با عرض 65.4px، آیکن درون‌خطی و مگامنوی فعال هم‌تراز باقی ماند.

### پایداری Hover مگامنو و انتقال دانش — ۷ سپتامبر ۲۰۲۶

- علت محوشدن فوری مگامنو شناسایی شد: قانون RTL پنل باز را 12px پایین می‌برد و میان آیتم «خدمات» و پنل شکاف ایجاد می‌کرد.
- در `rtl.css` حالت باز به `translateY(0)` تغییر کرد؛ در `beautia.js` مهلت خروج 320ms و در `polish.css` fade کنترل‌شده 200ms اضافه شد.
- سه فایل در هر هفت کپی قالب همگام و یکسان‌بودن هش‌ها تأیید شد؛ آزمون رفتاری نهایی مرورگر هنوز باز است.
- بنا به درخواست مالک، `CHATGPT-PROJECT-CONTEXT-FA.md`، `WORK-LOG-FA.md` و `UPLOAD-MANIFEST-FA.md` برای انتقال کامل دانش به پروژه ChatGPT ساخته شد.
