# QA Audits — فایل کامل تکه‌بندی‌شده

فایل اصلی `QA-AUDITS-CONSOLIDATED.json` بدون حذف محتوا در سه بخش قرار گرفته است.

برای بازسازی:

```bash
cat part-001.txt part-002.txt part-003.txt > ../QA-AUDITS-CONSOLIDATED.json
```

Arena هنگام تحلیل QA باید ترتیب همین سه فایل را رعایت کند.
