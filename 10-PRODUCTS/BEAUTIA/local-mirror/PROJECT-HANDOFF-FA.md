# سند تحویل و ادامه توسعه قالب بیوتیا

آخرین به‌روزرسانی: ۱۴۰۵/۰۶/۱۶ — 2026-09-07، منطقه زمانی Asia/Tehran

## ۱. هدف قطعی پروژه

بیوتیا یک قالب وردپرس فارسی و RTL برای کسب‌وکارهای زیبایی و رزرو خدمات است. محصول نهایی باید برای ارسال به مارکت راست‌چین آماده باشد، بدون وابستگی به افزونه پولی کار کند، با Elementor Free سازگار باشد، پنل تنظیمات کامل داشته باشد، دموهای واقعاً متفاوت اما از نظر تجربه و معماری یکپارچه ارائه کند و برای هر دمو بسته نصب آسان واقعی Duplicator بسازد.

هیچ بسته ساختگی، اسکریپت شبیه‌ساز Duplicator یا روش جایگزین بدون اجازه صریح مالک پروژه قابل قبول نیست.

## ۲. تصمیم برند

نام فعلی «بیوتیا / Beautia» حفظ شده است. نام کوتاه، خوش‌آوا، قابل برندشدن و مرتبط با Beauty است. تا زمانی که بررسی حقوقی/دامنه خلاف آن را نشان ندهد، تغییر نام لازم نیست.

## ۳. مسیرها و ساختار کاری

```text
C:\xampp\htdocs\beautia\
├── demoes\                         نصب‌های زنده و مستقل برای توسعه و QA
│   ├── nail\                      DB: beautia_nail
│   ├── clinic\                    DB: beautia_clinic
│   ├── spa\                       DB: beautia_spa
│   ├── hair\                      DB: beautia_hair
│   ├── makeup\                    DB: beautia_makeup
│   ├── lashes\                    DB: beautia_lashes
│   └── barber\                    DB: beautia_barber
├── demo-installers\               خروجی کاری Duplicator هر دمو
├── rtl-final-package\             تنها ریشه پکیج نهایی قابل ارسال
├── 01-Core\                       خروجی قدیمی؛ باید پس از همگام‌سازی نهایی پاک/ادغام شود
├── 02-Core-Demos\                 خروجی قدیمی؛ باید پس از همگام‌سازی نهایی پاک/ادغام شود
├── 03-Standalone-Duplicator\      خروجی قدیمی؛ باید پس از همگام‌سازی نهایی پاک/ادغام شود
├── 04-Documentation\              خروجی قدیمی؛ باید پس از همگام‌سازی نهایی پاک/ادغام شود
├── 05-Marketplace\                خروجی قدیمی؛ باید پس از همگام‌سازی نهایی پاک/ادغام شود
├── PROJECT-HANDOFF-FA.md           همین سند؛ مرجع ادامه کار
└── CHECKPOINT.json                 وضعیت ماشین‌خوان آخرین مرحله
```

آدرس‌های زنده:

- `http://localhost/beautia/demoes/nail/`
- `http://localhost/beautia/demoes/clinic/`
- `http://localhost/beautia/demoes/spa/`
- `http://localhost/beautia/demoes/hair/`
- `http://localhost/beautia/demoes/makeup/`
- `http://localhost/beautia/demoes/lashes/`
- `http://localhost/beautia/demoes/barber/`

## ۴. استاندارد بسته راست‌چین

راهنمای رسمی راست‌چین برای نصب آسان، بسته‌ای شامل دو فایل را توضیح می‌دهد:

1. `installer.php`
2. آرشیو ZIP ساخته‌شده توسط Duplicator

منبع: https://cloud.rtl-theme.com/blog/install-wordpress-theme-local-host/

پوشه کاری خروجی‌ها:

```text
demo-installers\<demo>\installer.php
demo-installers\<demo>\<duplicator-archive>.zip
```

نسخه نهایی همین ساختار باید در این مسیر همگام شود:

```text
rtl-final-package\03-Demo-Installers\<demo>\
```

پوشه `demoes` مخصوص توسعه است و نباید به‌عنوان سایت خام در پکیج فروش قرار گیرد.

## ۵. وضعیت فعلی دموها

هر هفت نصب:

- WordPress مستقل و دیتابیس مستقل دارند.
- `WP_HOME` و `WP_SITEURL` به مسیر همان دمو اشاره می‌کند.
- صفحه خانه مخصوص همان دمو فعال است.
- صفحه معرفی مرکزی در بسته‌های مستقل خاموش است.
- برگه‌های About/Services/Portfolio/Blog/Contact/Booking/Account/Login در ریشه قرار گرفته‌اند و URL دوبل ندارند.
- صفحه اصلی و `/booking/` با HTTP 200 تست شده‌اند.
- `dir="rtl"`، نبود سرریز افقی و نبود تصویر شکسته در Chrome بررسی شده است.
- عنوان و توضیح سایت برای هر دمو اختصاصی شده است.
- Duplicator رسمی نسخه 1.5.17 نصب و فعال شده است.

## ۶. ایزوله‌سازی محتوا

مشکل قبلی: هر دیتابیس همه محتوای هفت دمو را داشت و فقط در فرانت‌اند فیلتر می‌شد.

اصلاح انجام‌شده: هر دیتابیس اکنون فقط محتوای دمو، ۹ برگه نگاشت‌شده، یک منوی اختصاصی، تصاویر متصل و نوبت‌های خدمات همان دمو را نگه می‌دارد.

شمارش محتوای تخصصی پس از پاک‌سازی:

| دمو | خدمات | نمونه‌کار | متخصص | نظر | مقاله | رسانه |
|---|---:|---:|---:|---:|---:|---:|
| Nail | 6 | 6 | 3 | 3 | 3 | 18 |
| Clinic | 6 | 6 | 3 | 3 | 3 | 18 |
| Spa | 6 | 6 | 3 | 3 | 3 | 18 |
| Hair | 6 | 6 | 3 | 3 | 3 | 18 |
| Makeup | 6 | 6 | 3 | 2 | 3 | 18 |
| Lashes | 6 | 6 | 3 | 2 | 3 | 18 |
| Barber | 8 | 6 | 3 | 3 | 3 | 23 پس از ورود تصاویر جدید |

اصل ادامه کار: محتوای هر دمو باید هم از نظر رکورد دیتابیس و هم از نظر تصویر و متن مستقل باشد. فیلتر ظاهری به‌تنهایی کافی نیست.

## ۷. تفاوت و یکپارچگی دموها

یکپارچگی باید در معماری حفظ شود: الگوی رزرو، دسترس‌پذیری، کیفیت تایپوگرافی، ساختار پنل، سرعت، فاصله‌گذاری و قواعد ریسپانسیو.

تفاوت باید در این موارد واقعی باشد:

- هویت رنگ و کنتراست
- تایپوگرافی نمایشی و ریتم سکشن‌ها
- ترکیب‌بندی Hero و کارت‌ها
- تصاویر خدمات، نمونه‌کار و مقالات
- عنوان‌ها، متن‌ها، FAQ و لحن
- خدمات، مدت، قیمت، متخصص و برنامه زمانی
- نوع CTA و اولویت قابلیت‌ها
- جزئیات حرکت و جلوه‌های تعاملی

هویت‌ها:

- Nail: لطیف، هنری، صورتی/نود، جزئیات نزدیک و فرم‌های نرم
- Clinic: روشن، علمی، سبز-آبی، اعتماد و شفافیت
- Hair: ادیتوریال تیره، طلایی، حرکت و حجم
- Spa: طبیعی، تنفس‌پذیر، مریم‌گلی/سنگی، حرکت آهسته
- Lashes: نود و مینیمال، قاب نگاه و جزئیات ماکرو
- Makeup: جسور، رنگی، Berry/Cobalt، فضای کمپین و آکادمی
- Barber: زغالی، گردویی، برنز، دقیق و مردانه

## ۸. تصاویر و وضعیت فعلی

قانون قطعی: تصویر یک حوزه نباید در حوزه دیگر استفاده شود و یک تصویر نباید در چند کارت همان دمو تکرار شود.

ممیزی اولیه نشان داد تصاویر شاخص داخل هر دمو عمدتاً یکتا بودند، اما Seeder برای Spa، Lashes، Makeup و Barber تصاویر حوزه‌های دیگر را قرض می‌گرفت. اصلاح Barber جاری انجام شده و ۴ تصویر جدید تولید، بهینه و در قالب ذخیره شده‌اند:

- `assets/img/svc-barber-4.jpg`
- `assets/img/svc-barber-5.jpg`
- `assets/img/svc-barber-6.jpg`
- `assets/img/svc-barber-7.jpg`

این تصاویر در Media Library دموی Barber وارد و به خدمات 333، 334، 335 و 337 متصل شده‌اند.

تصاویر اختصاصی تازه تولید، به JPEG بهینه ۱۴۴۰×۹۶۰ با کیفیت ۸۴ تبدیل، در `assets/img` همه نصب‌ها همگام و به Media Library دموهای مربوط متصل شدند:

- Spa couples: `C:\Users\m\.codex\generated_images\01a072a9-4abd-7c40-9eab-77d0a355a11a\exec-03242d06-7c40-43c2-ba9f-1f9f85581e0e.png`
- Spa prenatal: `C:\Users\m\.codex\generated_images\01a072a9-4abd-7c40-9eab-77d0a355a11a\exec-a335cdcb-2b0e-4411-9e3c-2e6934c68034.png`
- Lashes lift: `C:\Users\m\.codex\generated_images\01a072a9-4abd-7c40-9eab-77d0a355a11a\exec-af96e509-6df1-4a27-89cb-0e444d55935c.png`
- Lashes lamination: `C:\Users\m\.codex\generated_images\01a072a9-4abd-7c40-9eab-77d0a355a11a\exec-72c18851-8c4e-48eb-92c7-a8b04d0dc116.png`
- Lashes mapping: `C:\Users\m\.codex\generated_images\01a072a9-4abd-7c40-9eab-77d0a355a11a\exec-c83ab7b2-b36c-481c-9c0d-88d070f2c5dd.png`
- Makeup bridal: `C:\Users\m\.codex\generated_images\01a072a9-4abd-7c40-9eab-77d0a355a11a\exec-ae529ea7-dcc2-4c46-9351-8a4869ac9056.png`
- Makeup editorial: `C:\Users\m\.codex\generated_images\01a072a9-4abd-7c40-9eab-77d0a355a11a\exec-821ac20e-f5a3-4a30-a79e-f46e5396368a.png`
- Makeup lesson: `assets/img/svc-makeup-5.jpg`
- Makeup academy: `assets/img/svc-makeup-6.jpg`

فایل‌های پروژه‌ای جدید عبارت‌اند از `svc-spa-5.jpg`، `svc-spa-6.jpg`، `svc-lash-4.jpg` تا `svc-lash-6.jpg` و `svc-makeup-3.jpg` تا `svc-makeup-6.jpg`. نگاشت `service_pools` در هر هفت کپی `inc/showcase-seed.php` اصلاح شد و دیگر تصویر بین‌حوزه‌ای ندارد. `folio_pools` فعلی هنوز تصاویر حوزه‌های دیگر دارد و قبل از بسته نهایی باید اصلاح شود.

## ۹. Elementor، ویجت‌ها و وابستگی‌ها

تصمیم معماری: هسته بومی و سریع + سازگاری اختیاری با Elementor Free. سایت بدون Elementor هم کامل کار می‌کند؛ در صورت نصب نسخه رایگان، ویجت‌های بیوتیا فعال می‌شوند. Elementor Pro نباید لازم باشد.

وضعیت فعلی:

- ۳۵ ویجت اختصاصی ثبت می‌شود.
- تست `elementor-smoke.php` اصلاح شد و PASS می‌دهد.
- مسیر قدیمی تست‌ها از `wordpress/wp-load.php` به `dirname(__DIR__, 3)/wp-load.php` اصلاح شد.
- جست‌وجوی کد وابستگی به Elementor Pro نشان نداد.
- صفحه Studio اگر با Elementor ویرایش شود محتوای Elementor را نمایش می‌دهد؛ در غیر این صورت ترکیب‌بندی بومی داینامیک رندر می‌شود.
- مگامنوی بومی بدون افزونه پولی در `inc/class-beautia-mega-menu.php` وجود دارد.

رقبای بررسی‌شده:

- صدف: ۳۰ ویجت Elementor، دو نوع مگامنو، رزرو، پنل پزشک، WooCommerce و پیامک را تبلیغ می‌کند.
- Magic Salon: بیش از ۱۵ ویجت، رزرو، زمان‌بندی خدمت، WooCommerce و پنل تنظیمات را تبلیغ می‌کند.
- بیوتیا از نظر تعداد ویجت و استقلال از افزونه پولی جلوتر است؛ اما کنترل‌های Style/Responsive هر ویجت باید در ادامه بررسی و در صورت نیاز گسترش یابد. صرف ثبت ۳۵ نام کافی نیست.

منابع تحقیق:

- https://www.rtl-theme.com/sadaf-wordpress-theme/
- https://www.rtl-theme.com/magic-salon-wordpress-theme/
- https://www.rtl-theme.com/tag/%D9%82%D8%A7%D9%84%D8%A8-%D8%B3%D8%A7%D9%84%D9%86-%D8%B2%DB%8C%D8%A8%D8%A7%DB%8C%DB%8C/

## ۱۰. پنل مدیریت مطلوب

منوی اصلی «بیوتیا» باید حداقل این زیرصفحه‌ها را یکپارچه کند:

1. داشبورد و وضعیت سیستم
2. نصب/درون‌ریزی دموها و Core Demo
3. هویت بصری: رنگ، فونت، فاصله، شعاع، سایه، حرکت
4. سربرگ، منو و مگامنو
5. فوتر
6. صفحات و ترتیب سکشن‌ها
7. خدمات، متخصصان و نمونه‌کار
8. رزرو: تقویم شمسی، ظرفیت، بافر، تعطیلات، لغو، بیعانه
9. پیامک/ایمیل با Gateway قابل تعویض
10. ورود موبایلی و حساب مشتری
11. عملکرد: Lazy Load، کیفیت/ابعاد تصویر، حذف CSS/JS غیرضروری، فونت محلی
12. یکپارچه‌سازی‌ها: WooCommerce، Elementor Free و درگاه
13. Import/Export/Reset تنظیمات با nonce و capability
14. مستندات و پشتیبانی

تمام ذخیره‌سازی‌ها باید sanitize، validate، nonce و capability check داشته باشند. هیچ کلید حساس نباید در خروجی عمومی یا سند تحویل ذخیره شود.

## ۱۱. QA الزامی پیش از ساخت Duplicator

برای هر دمو:

- صفحه اصلی و همه ۸ زیرصفحه HTTP 200
- لینک داخلی بدون 404 و بدون مسیر دوبل
- RTL و lang صحیح
- تست دسکتاپ، تبلت و موبایل
- بدون overlap دکمه بازگشت به بالا، رزرو سریع، چت و Sticky CTA
- بدون تصویر شکسته، تکراری یا مربوط به حوزه دیگر
- هر تصویر alt فارسی مناسب
- کنتراست متن روی تصویر حداقل قابل خواندن؛ به‌خصوص کارت «لیفت و رنگ مژه · دفتر الهام»
- منوی دسکتاپ، موبایل و مگامنو
- رزرو: خدمت ← متخصص ← تاریخ ← ساعت ← اطلاعات ← تأیید
- حساب مشتری، ورود، لغو و پیگیری
- نبود خطای PHP، JS و کنسول
- فعال بودن فقط افزونه‌های لازم
- غیرفعال بودن نمایش به موتور جستجو در بسته دمو
- حذف cache، log، backup قدیمی و داده تست حساس

## ۱۲. روند ساخت بسته واقعی Duplicator

1. QA همان دمو را تمام کن.
2. Duplicator رسمی را در پیشخوان همان نصب باز کن.
3. Create New / ساخت بسته جدید.
4. نام پایدار مانند `beautia-nail-4.0.0`.
5. اسکن را اجرا و هشدارها را بررسی کن؛ هشدار مهم نباید کورکورانه نادیده گرفته شود.
6. بسته کامل فایل + دیتابیس بساز؛ Database Only قابل قبول نیست.
7. `installer.php` و ZIP را دانلود/کپی کن.
8. در `demo-installers\<demo>` قرار بده.
9. checksum SHA-256 و اندازه را ثبت کن.
10. همان دو فایل را در `rtl-final-package\03-Demo-Installers\<demo>` همگام کن.
11. بسته را در یک دیتابیس و پوشه خالی آزمایشی نصب کن و صفحه اصلی/ورود/رزرو را تست کن.

## ۱۳. کارهای باز به ترتیب

1. تولید/اختصاص تصاویر Portfolio کاملاً حوزه‌محور و اصلاح `folio_pools`.
2. همگام‌سازی منبع قالب/افزونه میان هفت نصب و ساخت ZIP جدید Core.
3. تست بصری تک‌تک دموها در سه breakpoint و اصلاح کنتراست/overlap.
4. ممیزی کنترل‌های ۳۵ ویجت Elementor؛ افزودن کنترل‌های محتوا/استایل/Responsive در موارد کمبود.
5. تست کامل پنل ادمین و مگامنو.
6. ساخت هفت بسته واقعی Duplicator و نصب آزمایشی هر بسته.
7. تکمیل مستندات مشتری و دارایی‌های مارکت‌پلیس.
8. پاک‌سازی ریشه و قرار دادن فقط ساختار نهایی در `rtl-final-package`.

## ۱۴. قاعده همگام‌سازی

تا تعیین یک منبع توسعه واحد، پوشه `demoes\nail\wp-content\themes\beautia` منبع موقت تغییرات مشترک در نظر گرفته شده است. هر تغییر مشترک باید در شش نصب دیگر همگام شود. داده‌های دیتابیس و `wp-config.php` هر نصب نباید روی دیگری کپی شود.

پیش از ساخت ZIP Core باید QA files مانند `release-audit.php` و `elementor-smoke.php` طبق توضیح خودشان از ZIP تجاری حذف شوند، اما در مخزن توسعه باقی بمانند.

## ۱۵. وضعیت Usage و چک‌پوینت

پنجره Usage در 2026-09-06 ریست شد و ادامه کار از اصلاح تصاویر Portfolio آغاز شد. وقتی باقی‌مانده دوباره کمتر از ۱۰٪ شود، پس از هر تغییر معنادار این سند و `CHECKPOINT.json` باید فوراً به‌روز شوند.

## ۱۶. آخرین پیشرفت تصاویر Portfolio

- شش تصویر اختصاصی و بهینه‌شده `folio-clinic-1.jpg` تا `folio-clinic-6.jpg` تولید، در هر هفت نسخه قالب همگام، در کتابخانه رسانه دموی Clinic وارد و به شش نمونه‌کار همان دمو متصل شد.
- شش تصویر اختصاصی و بهینه‌شده `folio-spa-1.jpg` تا `folio-spa-6.jpg` تولید، در هر هفت نسخه قالب همگام، در کتابخانه رسانه دموی Spa وارد و به شش نمونه‌کار همان دمو متصل شد.
- تصاویر خروجی 1440×960، JPEG progressive با کیفیت 84 هستند؛ تصاویر Clinic و Spa از سرویس‌ها و حوزه‌های دموهای دیگر استفاده نمی‌کنند.
- `folio_pools` برای Clinic و Spa در هر هفت کپی `inc/showcase-seed.php` اصلاح شد تا نصب یا بازسازی دمو نیز همین دارایی‌های صحیح را برگرداند.
- گام فعال بعدی: تکمیل Portfolio اختصاصی Nail، Hair، Lashes، Makeup و Barber و سپس ممیزی تصاویر مقالات که هنوز در `article_pools` ارجاع‌های میان‌حوزه‌ای دارند.

## ۱۷. ترتیب قطعی اجرای باقی‌مانده

1. تکمیل Core مشترک و استقلال واقعی هر هفت دمو: دیتابیس، محتوا، عکس، پالت، هویت، هاور، آیکن، هدر، منوی RTL، مگامنو، پنل تنظیمات و جستجوی هوشمند.
2. بازدید زنده و Responsive از تمام صفحات هر هفت دمو و رفع همه خطاهای باقی‌مانده.
3. ساخت هفت بسته واقعی Duplicator و همگام‌سازی هر `installer.php` و archive در `demo-installers/<demo>` و `rtl-final-package/03-Demo-Installers/<demo>`.
4. ساخت دموی مادر و لندینگ انتخاب دموها در `http://localhost/beautia/` پس از نهایی‌شدن دموها.

### پیشرفت جاری مرحله اول

- علت اصلی تداخل رنگ پیدا و رفع شد: سه لایه آزمایشی `reference-luxury.css`، `pastel-studio.css` و `glam-refinement.css` دیگر هم‌زمان با هویت دمو بارگذاری نمی‌شوند.
- Core اکنون فقط CSS پایه، CSS مختص همان دمو و یک لایه هویت کنترل‌شده را بارگذاری می‌کند.
- مگامنوی بومی بدون افزونه پولی با مدل Editorial، شش کارت خدمت، تصویر محلی و آیکن برای هر هفت دمو ساخته و فعال شد.
- هدر جدید شامل «ورود / عضویت» متنی در دسکتاپ، آیکن صحیح حساب در موبایل، همبرگر و دکمه بستن منوی موبایل است.
- جستجوی AJAX هوشمند و محدود به همان دمو، با debounce، تصویر، نوع نتیجه، دکمه بستن و ESC پیاده شد. آزمون واقعی دموی Hair برای «کو» نتیجه اول «کوتاهی تخصصی و استایل» را با HTTP 200 برگرداند.
- قانون کنتراست هاور دکمه و وسط‌چینی SVG/آیکن به Core اضافه شد.
- منشأ متن خام `\\n\\n` پیدا شد: دو `printf` مربوط به preload فونت از رشته تک‌کوتیشن استفاده می‌کردند. به newline واقعی اصلاح و در مرورگر تأیید شد که صفحه دیگر با متن خام شروع نمی‌شود.
- تگ تصویر خالی Lightbox حذف شد؛ تصویر فقط هنگام بازشدن Lightbox ساخته و هنگام بستن پاک می‌شود. شمار تصاویر شکسته در تست زنده Hair از ۱ به ۰ رسید.
- Seeder دیگر صفحات را فرزند صفحه خانه نمی‌کند. تمام صفحات فعلی هفت نصب نیز به URL سطح اول تبدیل شدند و `/about/` در هر هفت دمو HTTP 200 دارد.
- تست زنده Hair پس از اصلاحات: RTL فعال، مگامنو فعال، «ورود / عضویت» صحیح، overflow افقی صفر، تصویر شکسته صفر و متن خام ابتدای صفحه صفر.

### توقف درخواستی کاربر — چک‌پوینت تصاویر

کاربر در میانه مرحله اول درخواست توقف کرد. پیش از توقف، خروجی‌های تولیدشده داخل پروژه تثبیت شدند:

- `folio-hair-2.jpg` تا `folio-hair-6.jpg`: پنج تصویر جدید و اختصاصی پورتفولیوی مو آماده است. آیتم اول می‌تواند از `glam-hair.jpg` استفاده کند؛ هنوز `folio_pools` و تصاویر شاخص دیتابیس Hair به مجموعه جدید تغییر نکرده‌اند.
- `folio-nail-5.jpg` و `folio-nail-6.jpg`: دو جایگزین جدید برای دو تصویر میان‌حوزه‌ای پورتفولیوی Nail آماده است؛ هنوز به دیتابیس و Seeder متصل نشده‌اند.
- `folio-lashes-1.jpg` و `folio-lashes-2.jpg`: دو تصویر نخست پورتفولیوی Lashes آماده است؛ چهار تصویر دیگر لازم است و اتصال نهایی هنوز انجام نشده است.
- همه تصاویر بالا 1440×960، JPEG progressive و کیفیت 84 هستند و در assets هر هفت کپی قالب همگام شده‌اند.
- اقدام بعدی دقیق: تولید `folio-lashes-3..6`، سپس اتصال کامل Nail/Hair/Lashes به Seeder و دیتابیس، بعد Makeup و Barber و در پایان تصاویر اختصاصی مقالات.

## ۱۸. ادامه پس از چک‌پوینت — ۷ سپتامبر ۲۰۲۶

وضعیت توقف قبلی منقضی شده و کار با وضعیت `in_progress` ادامه دارد. نتیجه‌های تثبیت‌شده:

- پورتفولیوی Nail، Hair و Lashes هر کدام شش تصویر حوزه‌ای و مستقل دارند؛ تصاویر به دیتابیس همان دمو متصل و `folio_pools` اصلاح شده است.
- برای Makeup شش تصویر جدید `folio-makeup-1.jpg` تا `folio-makeup-6.jpg` ساخته، با خروجی 1440×960/JPEG progressive/quality 84 بهینه، در هر هفت Core همگام و در دیتابیس Makeup ثبت شد.
- برای Barber شش تصویر جدید `folio-barber-1.jpg` تا `folio-barber-6.jpg` با همین استاندارد ساخته، همگام و در دیتابیس Barber ثبت شد.
- برای مقالات Nails، Clinic، Hair و Spa دوازده تصویر اختصاصی `article-<demo>-1..3.jpg` ساخته و در Core ذخیره شد.
- شش خروجی مقالات Lashes و Makeup در Core هر هفت نصب همگام و به تصاویر شاخص مقالات دیتابیس‌های مربوط متصل شدند.
- سه تصویر اختصاصی `article-barber-1.jpg` تا `article-barber-3.jpg` با استاندارد 1440×960/JPEG progressive/quality 84 ساخته، در هر هفت Core همگام و به دیتابیس Barber متصل شد.
- تصاویر اختصاصی هر ۲۱ مقاله در دیتابیس مربوط ثبت شدند و `article_pools` هر هفت کپی Seeder فقط به دارایی‌های حوزه همان دمو ارجاع می‌دهد.
- مصرف پنجره پنج‌ساعته هنگام این چک‌پوینت ۲۴٪ و مانده ۷۶٪ است؛ الزام ذخیره لحظه‌ای زیر ۱۰٪ مانده همچنان برقرار است.

چک‌لیست فعال باقی‌مانده:

- [x] بازگرداندن فونت‌ها به ظاهر قبلی در هر هفت دمو و تأیید زنده دسکتاپ/موبایل
- [x] رفع به‌هم‌ریختگی هدر، منوی دسکتاپ، مگامنو و منوی موبایل در هر هفت دمو و تأیید زنده
- [x] تحقیق به‌روز درباره قالب‌های برتر سالن زیبایی/رزرو و ثبت الگوهای موفق با منبع
- [ ] مقایسه هفت دمو از نظر تعداد و نوع المان‌ها، سکشن‌ها، ویجت‌ها، ترتیب محتوا، Hero، کارت‌ها، CTA و قابلیت‌ها
- [ ] تصمیم‌گیری و اجرای تنوع واقعی چیدمان و امکانات هر دمو با حفظ Core، استانداردهای مشترک و تجربه یکپارچه
- [ ] بازبینی تعداد و نوع ویجت‌های Elementor هر دمو بر اساس نیاز حوزه و الگوهای برتر، نه صرفاً یک فهرست یکسان
- [ ] کنترل هم‌ترازی (alignment) تمام متن‌ها، آیکن‌ها، دکمه‌ها، کارت‌ها و فرم‌ها در دسکتاپ، تبلت و موبایل
- [ ] کنترل Justify مناسب متن‌های بلند فارسی و جلوگیری از فاصله‌های بد، کشیدگی یا ناهمگونی خطوط
- [ ] کنترل نظم بصری، فاصله‌گذاری، گرید، عرض‌ها و ریتم عمودی تمام صفحات
- [ ] کنترل رنگ و خوانایی همه متن‌ها در حالت روشن/تیره، روی تصویر، کارت، دکمه و حالت hover/focus؛ هیچ متن بی‌رنگ یا محو نباشد
- [ ] جلوگیری از قرارگرفتن ناخواسته المان‌ها روی هم در تمام breakpointها
- [ ] ممیزی هم‌پوشانی‌های عمدی، stacking context و `z-index` هدر، مگامنو، مودال، جستجو، چت، رزرو سریع و Sticky CTA
- [ ] کنترل کنتراست و تناسب رنگ متن با پس‌زمینه در تمام سکشن‌ها، کارت‌ها، فرم‌ها، تصاویر، مودال‌ها و حالت‌های تعاملی
- [ ] کنترل هماهنگی رنگ تمام اجزا شامل متن، پس‌زمینه، حاشیه، آیکن، دکمه، تصویر، سایه، hover، focus، disabled و حالت روشن/تیره
- [ ] بازبینی هنری و طراحی تمام هفت دمو و کل Core برای خروجی مرتب، منظم، حرفه‌ای، شیک، زیبا و لوکس؛ نبود خطای فنی به‌تنهایی معیار قبولی نیست
- [ ] حفظ هویت رنگی مستقل هر دمو در کنار یک استاندارد لوکس و منسجم در Core
- [ ] کنترل نظم، اندازه، فاصله و هم‌ترازی تمام آیکن‌ها داخل دکمه‌ها، لینک‌ها، ورودی‌ها، کارت‌ها و سایر المان‌ها
- [ ] کنترل وضوح و تناسب رنگ تمام آیکن‌ها با پس‌زمینه در حالت‌های normal، hover، focus، active و disabled
- [ ] تمام‌عرض بودن ساختار اصلی هر هفت دمو در دسکتاپ و حذف قاب، حاشیه یا محدودیت عرض ناخواسته؛ محتوا داخل container استاندارد باقی بماند
- [ ] استقلال کامل و قابل اثبات هر هفت دمو با معیار صفر تداخل محتوایی
- [ ] ممیزی جداگانه متن، تصویر، خدمات، متخصصان، نمونه‌کار، مقاله، نظر مشتری، قیمت، زمان‌بندی، منو، FAQ، CTA، تنظیمات و رکوردهای دیتابیس هر دمو
- [ ] جلوگیری از هرگونه قرض‌گرفتن دارایی، fallback میان‌حوزه‌ای یا نمایش رکورد متعلق به دموی دیگر در فرانت‌اند، Seeder و دیتابیس
- [ ] راست‌چین‌بودن کامل منوی دسکتاپ، مگامنو، زیرمنوها و منوی موبایل، شامل ترتیب، تراز متن، آیکن و جهت فلش‌ها
- [ ] کنترل تناسب اندازه تمام آیتم‌های منو: ارتفاع، اندازه فونت، padding، فاصله، خط پایه و سطح کلیک
- [x] هم‌اندازه و هم‌ترازکردن آیتم «خدمات» با سایر آیتم‌ها، در حالی که آیکن و قابلیت مگامنو حفظ می‌شود
- [ ] تأیید نهایی Hover Intent مگامنو در هر هفت دمو؛ حذف شکاف RTL و تأخیر 320ms پیاده شده اما آزمون مرورگری نهایی باز است
- [ ] نگهداری مرحله‌ای همه سورس‌ها، گزارش‌ها، فهرست فایل‌ها و چک‌پوینت در پروژه ChatGPT «قالب زیبایی بیوتیا»
- [x] انتقال و اتصال تصاویر مقالات Lashes و Makeup
- [x] ساخت و اتصال سه تصویر مقالات Barber
- [x] حذف کامل ارجاع‌های میان‌حوزه‌ای از `article_pools`
- [ ] ممیزی هش/تصویر شکسته/تکراری و محتوای اشتباه هر هفت دیتابیس
- [ ] تکمیل و ممیزی پنل تنظیمات فول‌داینامیک و کنترل‌های Elementor Free
- [ ] QA زنده دسکتاپ/موبایل همه صفحات هر هفت دمو
- [ ] ساخت و آزمون هفت بسته رسمی Duplicator
- [ ] همگام‌سازی بسته‌ها در دو مسیر تحویل
- [ ] ساخت و QA دموی مادر در `/beautia/`
- [ ] تکمیل کاور، آیکن، اینفوگرافیک، توضیحات محصول و بسته نهایی راست‌چین

### پیشرفت اصلاح فونت، استایل و منو — ۷ سپتامبر ۲۰۲۶

- علت غیرفعال‌بودن هویت‌های بصری مشخص شد: `demo-identities.css` به `data-beautia-appearance="pastel"` وابسته بود اما این ویژگی روی سند تولید نمی‌شد. ویژگی به‌صورت کنترل‌شده برای صفحات دمو اضافه شد.
- `@font-face` فونت‌های Sahel و Gandom به `fonts.css` مشترک منتقل شد؛ دیگر فونت هویت‌ها به یک فایل بصری اختیاری وابسته نیست.
- تقدم `front-page.php` باعث می‌شد قالب صفحه Studio نادیده گرفته و ساختار قدیمی و یکسان نمایش داده شود. صفحه اصلی اکنون انتساب صریح Studio را رعایت می‌کند.
- cache bust مبتنی بر زمان تغییر فایل برای فونت، لایه polish، هویت دمو و اسکریپت اصلی اضافه شد تا مرورگر CSS/JS قدیمی را نگه ندارد.
- هدر روشن و خوانا، فاصله منظم، RTL، `z-index` پایدار و مگامنوی قابل استفاده با Hover/Focus پیاده شد.
- آیکن آیتم «خدمات» کوچک و درون‌خطی شد؛ اندازه تمام شش آیتم منوی دسکتاپ در هر هفت دمو 43.3px و یکسان است.
- منوی موبایل Hair در عرض 390px تست شد: شش آیتم اصلی ارتفاع یکسان 68.55px، RTL صحیح، overflow صفر و تصویر شکسته صفر دارد. هنگام بازبودن منو، Chat/FAB/Sticky CTA پنهان می‌شوند و `aria-hidden` به‌روز می‌شود.
- ممیزی خودکار صفحه اصلی هر هفت دمو در عرض 390 و 1440 پیکسل: فونت هویت فعال، هدر خوانا، overflow صفر و تصویر شکسته صفر. پوسته صفحه با viewport هم‌اندازه و تمام‌عرض است.
- تفاوت ساختاری فعلی کافی نیست: Nail دارای 11 سکشن و شش دموی دیگر دارای 10 سکشن با ترتیب تقریباً یکسان‌اند. تحقیق و طراحی تنوع حوزه‌محور همچنان باز است.

### تنوع حوزه‌محور و حذف تداخل محتوایی — ۷ سپتامبر ۲۰۲۶

- تحقیق رقبا و تصمیم معماری در `COMPETITOR-RESEARCH-FA.md` ثبت شد؛ نتیجه: Core مشترک می‌ماند اما ترتیب روایت، تعداد کارت‌ها و ترکیب قابلیت‌ها باید حوزه‌محور باشد.
- پروفایل مستقل صفحه اصلی برای هفت دمو پیاده شد. ترتیب سکشن‌ها و تعداد خدمات، نمونه‌کارها، اعضای تیم و مقالات دیگر یکسان نیست.
- Clinic با اعتماد و معرفی، Hair و Makeup با Portfolio، Spa با تجربه، Lashes با گالری فرم‌ها، Barber با خدمات و Nails با خدمات/مقایسه شروع می‌شوند.
- تداخل واقعی تصاویر بخش «درباره» حذف شد. هر دمو اکنون فقط Hero و Portfolio متعلق به حوزه خودش را در این بخش استفاده می‌کند.
- عنوان و سابقه بخش «درباره» نیز برای هر حوزه مستقل شد.
- این مرحله «نسخه اول تنوع ساختاری» است؛ ویجت‌های تخصصی مرحله دوم هنوز در چک‌لیست باز هستند.
- خروجی جدید در مرورگر هر هفت دمو تأیید شد: ترتیب سکشن‌ها متفاوت، عنوان و دو تصویر About کاملاً هم‌حوزه، overflow افقی صفر و تصویر واقعاً شکسته صفر است.
- منوی موبایل هر هفت دمو در عرض 390px جداگانه باز و اندازه‌گیری شد: جهت RTL، شش آیتم با ارتفاع یکسان 68.55px، آیتم «خدمات» دقیقاً هم‌اندازه بقیه، `aria-hidden=false` هنگام بازبودن و FAB پنهان است.
- سکشن تخصصی مستقل برای هر دمو ساخته شد: راهنمای طرح Nails، مسیر مراجعه Clinic، انتخاب مهارت Hair، آیین Spa، نقشه فرم چشم Lashes، پکیج مراسم Makeup و باشگاه Barber. تعداد کارت، محتوا، جایگاه و فرم بصری آن‌ها یکسان نیست.
- سکشن‌های تخصصی در عرض‌های 1280 و 390 پیکسل تست شدند: overflow صفر و نسبت کنتراست متن/کارت بین 9.17 و 11.56 است. متن کم‌رنگ Hair در همین ممیزی پیدا و اصلاح شد.
- ممیزی فعلی دیتابیس نشان داد تمام تصاویر خدمات، Portfolio و مقاله اکنون حوزه‌محورند؛ تنها تداخل تصویری باقی‌مانده، سه تصویر عمومی اعضای تیم است که بین چند دمو تکرار شده و باید با پرتره‌های اختصاصی جایگزین شود.
- مورد تصاویر تیم نیز بسته شد: هفت Triptych حوزه‌محور به ۲۱ پرتره 720×960/JPEG progressive تبدیل، در Assets هر هفت Core همگام، به رکوردهای Staff همان دیتابیس متصل و `staff_photo_pools` Seeder اصلاح شد.
- ممیزی دیتابیس پس از اتصال پرتره‌ها برای تصویر Service/Portfolio/Staff/Post مقدار `cross_demo_duplicate_images=0` داد. هر ۲۱ تصویر تیم نیز در مرورگر با ابعاد واقعی و بدون شکستگی بارگذاری شد.
- محتوای غیرتصویری تمام Testimonialها نیز هش شد و تکرار میان‌دمویی نداشت. سه محتوای تکراری باقی‌مانده مربوط به صفحه‌های Services/Portfolio/Blog بود که فقط Shortcode مشترک داشتند؛ برای هر دمو معرفی حوزه و نام مجموعه اضافه و Seeder نیز اصلاح شد.
- ممیزی نهایی محتوای غیرخالی Post/Page/CPT اکنون `duplicate_nonempty_content_groups=0` و `literal_slashes=0` می‌دهد.
- ۲۱ صفحه Services/Portfolio/Blog در مرورگر باز شد: عنوان مستقل، overflow صفر و تصویر شکسته صفر.
- ۳۵ صفحه About/Contact/Booking/Account/Login در هر هفت دمو، یک بار دسکتاپ و یک بار موبایل (۷۰ حالت) ممیزی شد: RTL صحیح، overflow صفر، تصویر شکسته صفر و هم‌پوشانی Sticky CTA/Support صفر.
- سطح کلیک آیتم‌های کوتاه منوی دسکتاپ حداقل 44px شد. در تأیید زنده، هر شش آیتم ارتفاع 43.3px دارند؛ «خدمات» با عرض 65.4px، آیکن درون‌خطی و مگامنوی فعال هم‌تراز باقی ماند.

### پایداری Hover مگامنو و انتقال دانش — ۷ سپتامبر ۲۰۲۶

- علت محوشدن فوری مگامنو شناسایی شد: قانون RTL پنل باز را 12px پایین می‌برد و میان آیتم «خدمات» و پنل شکاف ایجاد می‌کرد.
- در `rtl.css` حالت باز به `translateY(0)` تغییر کرد؛ در `beautia.js` مهلت خروج 320ms و در `polish.css` fade کنترل‌شده 200ms اضافه شد.
- سه فایل در هر هفت کپی قالب همگام و یکسان‌بودن هش‌ها تأیید شد؛ آزمون رفتاری نهایی مرورگر هنوز باز است.
- بنا به درخواست مالک، `CHATGPT-PROJECT-CONTEXT-FA.md`، `WORK-LOG-FA.md` و `UPLOAD-MANIFEST-FA.md` برای انتقال کامل دانش به پروژه ChatGPT ساخته شد.
- انتقال کامل انجام شد: 20 Source شامل 9 آرشیو کامل (ریشه، دیتابیس‌ها و 7 نصب زنده) و 11 سند خوانا/فنی/QA در پروژه ChatGPT «قالب زیبایی بیوتیا» از روی صفحه Sources تأیید شدند.

### توقف به درخواست کاربر — ۷ سپتامبر ۲۰۲۶، ساعت ۱۸:۰۰ تهران

- کاربر خواست کار تا تازه‌شدن محدودیت متوقف شود؛ هیچ مرحله جدیدی شروع نشد.
- وضعیت انتقال: 20 Source در پروژه ChatGPT قابل مشاهده است؛ شامل بسته ریشه، هفت نصب کامل، dump واقعی هفت دیتابیس و اسناد دانش/QA.
- اصلاح Hover Intent در هر هفت کپی کد همگام است. تست واقعی Nail نشان داد کلاس `is-hovering` در مهلت خروج حفظ و سپس حذف می‌شود؛ چون روش drag به لینک فوکوس داد، `focus-within` پنل را عمداً باز نگه داشت. بنابراین مورد فقط پس از تست مجدد بدون فوکوس یا با blur بسته شود.
- نقطه ادامه دقیق: پایان QA مگامنو در هفت دمو، سپس ممیزی هنری/کنتراست صفحات جزئیات طولانی.
